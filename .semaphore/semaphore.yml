version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
  containers:
    - name: main
      image: 'registry.semaphoreci.com/node:14-browsers'
blocks:
  - name: test
    task:
      prologue:
        commands:
          - checkout
          - yarn install
      jobs:
        - name: run tests
          commands:
            - yarn ci
