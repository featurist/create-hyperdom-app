version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: test
    task:
      jobs:
        - name: run tests
          commands:
            - sem-version node 14.5
            - checkout
            - yarn install
            - yarn ci
